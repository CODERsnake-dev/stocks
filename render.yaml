services:
  - type: web
    name: stock-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DB_PATH
        value: /data/portfolio.db
    disk:
      name: portfolio-data
      mountPath: /data
      sizeGB: 1
